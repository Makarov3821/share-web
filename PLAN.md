# Share Web 实现计划

## 项目目标

构建一个与运行平台解耦的局域网文本和文件共享服务。服务本身不依赖 Docker、OpenWrt 专用网络规则或固定目录，既可以运行在 ImmortalWrt，也可以迁移到 NAS 或普通 Linux 主机。

访问域名（例如 `share.f`）由局域网 DNS 或反向代理负责解析；应用只负责监听 HTTP 端口和提供页面/API。

## 总体架构

```text
浏览器
  │
  ├── share.f 的局域网 DNS / 反向代理
  │
  └── share-web（Rust 单体服务）
        ├── 原生 HTML/CSS/JavaScript 页面
        ├── 文件和剪切板 API
        ├── 过期清理任务
        └── 可配置的数据目录
```

### 技术选择

- 后端：Rust。
- HTTP：axum + tokio。
- JSON：serde。
- 前端：原生 JavaScript/CSS，静态资源编译进程序。
- 数据：第一版使用可迁移的 JSON 元数据文件和独立对象目录，避免依赖系统 SQLite/OpenSSL，降低 OpenWrt 交叉编译难度。
- 部署：普通二进制；ImmortalWrt 使用 procd，NAS/Linux 使用 systemd 或其他进程管理器。

## 配置约定

第一版通过命令行配置：

```text
--listen             监听地址，默认 0.0.0.0:8080
--data-dir           持久化数据目录，默认 ./data
--max-total-size     总使用空间上限，0 表示不限制，例如 100G、10GiB
--max-upload-size    单文件上限，0 表示不限制
--max-clip-size      单条文字上限，默认 1 MiB
--cleanup-interval   过期清理间隔，默认 60 秒
```

目录不应该由网页端任意修改。部署者通过参数选择挂载盘目录，网页只管理共享内容。

数据目录格式：

```text
data-dir/
├── metadata.json
├── objects/
└── tmp/
```

整个 `data-dir` 可以复制到另一台机器完成迁移。文件实际名称使用随机 ID，用户原始文件名只用于显示和下载响应头。

## 第一版 MVP

### 后端功能

- `GET /api/items`：获取文件和剪切板列表及空间用量。
- `POST /api/files`：multipart 文件上传。
- `GET /api/files/:id/download`：文件下载。
- `POST /api/clips`：创建文字卡片。
- `PATCH /api/items/:id`：修改过期时间。
- `DELETE /api/items/:id`：删除文件或文字卡片。
- `GET /api/health`：健康检查。
- 文件写入临时目录，成功后原子移动到对象目录。
- 后端执行总空间限制和单文件/单文字限制。
- 后台定期清理过期项目，列表和下载时再次惰性检查过期状态。

### 前端功能

- 桌面端文件和剪切板两列卡片布局，窄屏自动变为单列。
- 文件拖拽上传和文件选择上传。
- 点击文件卡片主体下载。
- 全局 `Ctrl+V`/`Command+V` 将纯文本创建为新的文字卡片。
- 点击文字卡片复制内容，兼容非 HTTPS 内网页面。
- 每张卡片提供设置过期时间按钮和右上角删除按钮。
- 显示创建时间、大小和剩余有效时间。
- 上传进度、错误提示和空间用量提示。

## 分阶段路线

### 阶段一：跨平台核心服务（当前）

- 创建 Rust Cargo 项目。
- 实现配置解析和可读的容量参数（`G`、`GiB` 等）。
- 实现数据目录、元数据持久化、对象文件管理。
- 实现文件上传、下载、文字创建、列表、修改过期时间和删除。
- 实现总使用空间限制及过期清理。
- 嵌入静态网页并提供健康检查。

### 阶段二：页面体验

- 完成卡片 UI、拖拽上传、剪切板粘贴和复制。
- 增加响应式布局、进度反馈和错误恢复。
- 验证大文件、中文文件名、多行文本和移动端行为。

### 阶段三：平台部署

- 提供 ImmortalWrt procd init 脚本和配置示例。
- 提供 NAS/Linux systemd 服务示例。
- 提供 dnsmasq 的 `share.f` 解析示例。
- 提供 Nginx/Caddy 反向代理示例。
- 在实际设备上验证持久化挂载盘和服务重启恢复。

### 阶段四：安全与运维增强

- IP/CIDR 访问限制，同时支持 IPv4/IPv6。
- 上传总量、文件数量和速率限制。
- 可选 PIN/密码或只读模式。
- HTTPS、备份/恢复、孤儿文件扫描。
- 图片预览、搜索、批量操作和分享二维码。

## 设计注意事项

- 默认不启用认证时，所有能访问页面的局域网客户端都拥有读取、上传和删除权限。
- 数据目录必须位于持久化挂载盘，避免使用 `/tmp` 或频繁写入路由器闪存。
- 文件对象不直接暴露为静态目录，下载必须经过 API 校验过期状态。
- 文件名必须进行路径和响应头安全处理；文字始终按纯文本渲染，不能当作 HTML 插入。
- 服务器时间负责过期判断，浏览器只负责显示倒计时。
- 应用不修改 DNS、防火墙或透明代理规则；这些属于部署环境职责。
- 同一个数据目录只允许一个服务实例使用，避免元数据并发写入。
